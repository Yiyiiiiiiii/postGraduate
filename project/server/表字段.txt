用户表：
id 账号 密码 姓名 年龄 性别 手机号 余额（默认值为0） 角色（0：用户 1：管理员） 头像 信誉值（默认值为5）

餐品：
id 名称 价格 库存 图片

订单：
id user_id food_id 时间 状态（0:未出餐 1：已出餐）

二手书：
id 书名 图片 类型 库存

借阅表：
id user_id book_id  状态（0：未归还 1：捐赠 2：归还）=》状态为0，二手书库存-1  状态为1和2，二手书库存+1）

座位：
id 座位号 座位类型 价格 剩余座位数(丢弃) 状态(0：可选 1：已选)

订座：
id user_id seat_id start_time end_time 状态（0：未打卡 1：已打卡）==》信誉分通过状态计算得出（订座页面信誉分为负数一个星期不可订座）默认值5分

完善：图片反显 侧边栏 个人中心查看更多 捐赠和归还图书需管理员操作

问题：
     
     